{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Yarp": "Information"
    }
  },
  "Cors": {
    "WebOrigin": "https://your-web-app.azurewebsites.net",
    "MobileOrigin": "https://your-mobile-app.azurewebsites.net"
  },
  "ReverseProxy": {
    "_comment": "Intentionally empty — see appsettings.json. The previous sample 'bff-*' routes self-proxied to the gateway on the same path and looped. In production the empty overrides also ensure the base-config sample routes cannot re-introduce the loop via config layering. Add real downstream routes here when a genuine BFF backend exists.",
    "Routes": {},
    "Clusters": {}
  }
}

