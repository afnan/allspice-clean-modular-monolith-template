# Phase 3 — DRY & Code Smells Implementation Plan

> Stacks on merged Phase 2 (`main`). Same flow: TDD / regression-test per task → one Phase 3 PR → independent review → fix → merge. No primary-constructor sweep (that is Phase 4).

**Goal:** Close the DRY / code-smell findings (D1–D10) from the 2026-06-23 review, re-scoped against the current code (invite-user removed; `HttpContextCurrentUserProvider` reworked in F-identity).

**Constraints:** .NET 10, warnings-as-errors, central package versions, no AI co-author trailer. Behaviour-preserving refactors get a regression test that passes before and after; behavioural changes (D6, D8) get a fail-first test.

## Re-scope

| Item | Status | Change |
|------|--------|--------|
| **D1** | valid | Extract a shared email-envelope builder used by MailKit/Resend/SendGrid senders (From/ReplyTo/HTML-vs-text). |
| **D2** | valid | `ArdalisResultHttpExtensions`: share one `ResultStatus → problem` mapper between the `Result` and `Result<T>` failure paths. |
| **D3** | re-layered | Provider no longer duplicates claim reading (F-identity). Consolidate the remaining `sub`/NameIdentifier fallback chain (Web `ClaimsPrincipalExtensions.GetUserObjectId`, `RealTime/AppHub.GetUserId`) into one helper in `Identity.Abstractions/Claims`; fix AppHub's missing `client_id` fallback drift. |
| **D4** | MOOT | `InvitationRepository` removed; only `UserRepository` uses the escaped-ILIKE idiom. No duplication. Skip. |
| **D5** | valid | Split role operations out of `KeycloakDirectoryClient` (339 lines) into a `KeycloakRoleClient`; factor the repeated `EnsureSuccessStatusCode` + `JsonDocument.ParseAsync` boilerplate into private helpers. |
| **D6** | valid | `PerformanceBehavior`: add `PerformanceBehaviorOptions.SlowRequestThresholdMs`; log a warning when exceeded, else trace. |
| **D7** | valid | `NotificationDailyDigestJob` is a stale-pending monitor, not a digest. Rename to reflect that + document; no behaviour change. |
| **D8** | valid | `EmailSenderDispatcher`: in non-Development, fail fast instead of silently falling back to MailKit/localhost when no real provider is configured. |
| **D9** | valid | Remove redundant `?? "admin"` after `GetParameter`/`GetSecret` in `AppHost.cs` (defaults already upstream). |
| **D10** | valid | Remove the redundant `failure is not null` filter in `ValidationBehavior`. |
| **D11** | done | Resolved by Phase 0 (repos now use `EfRepository`). |

## Sequencing
Mechanical/low-risk first (D10, D9, D2, D6, D7), then behavioural (D8), then refactors with regression coverage (D3, D1, D5). D5 lands last (largest). One commit per item.

## Definition of Done
Build clean (0 warnings) + full suite green (Docker for Testcontainers). One Phase 3 PR; independent review; fix; merge.
